{
  "name": "mongodb_tuts",
  "version": "1.0.0",
  "description": "this is how we can use mongo in node ",
  "main": "Inserting_data_ in_database.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "arif",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^6.2.7"
  }
}
